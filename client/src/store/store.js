import { configureStore } from '@reduxjs/toolkit';

// Placeholder store - you can expand this with actual slices
const store = configureStore({
  reducer: {
    // Add your reducers here
  },
});

export { store };